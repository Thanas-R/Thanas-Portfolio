// Contour flow background — temporarily disabled, rendering plain bg
const TopographicBackground = () => {
  return <div className="fixed inset-0 w-full h-full bg-background" style={{ zIndex: 0 }} />;
};

export default TopographicBackground;